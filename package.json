{
  "devDependencies": {
    "cypress": "^13.12.0"
  },
  "scripts": {
  "cy:open": "npx cypress open --env environment=dev",
  "cypress:execution": "npx cypress run",
    "cy:run": "npx cypress run"
}

}
